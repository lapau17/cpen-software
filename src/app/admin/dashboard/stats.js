"use client";
import { useEffect, useState } from "react";
import AdminShell from "@/components/AdminShell";
const money = (v) => new Intl.NumberFormat("en-GH", { style: "currency", currency: "GHS" }).format(Number(v || 0));
export default function AdminDashboard() { const [stats, setStats] = useState(null); useEffect(() => { fetch("/api/admin/stats").then((r) => r.json()).then((b) => setStats(b.data)); }, []); if (!stats) return <AdminShell><p>Loading departmental statistics…</p></AdminShell>; const cards = [["Total students", stats.total_students], ["Total courses", stats.total_courses], ["Total enrollments", stats.total_enrollments], ["Total lecturers", stats.total_lecturers], ["Teaching assistants", stats.total_teaching_assistants], ["Fees due", money(stats.total_fees_due)], ["Payments", money(stats.total_payments)], ["Outstanding", money(stats.total_outstanding)]]; return <AdminShell><header className="page-title"><p className="eyebrow">Department overview</p><h1>Admin dashboard</h1></header><section className="stat-grid">{cards.map(([label, value]) => <article className="summary-card" key={label}><span>{label}</span><strong>{value}</strong></article>)}</section></AdminShell>; }
