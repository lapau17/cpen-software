"use client";
import Link from "next/link";
import { useEffect, useState } from "react";
import AdminShell from "@/components/AdminShell";
const money = (v) => new Intl.NumberFormat("en-GH", { style: "currency", currency: "GHS" }).format(Number(v || 0));
export default function StudentDirectory() { const [search, setSearch] = useState(""); const [students, setStudents] = useState([]); const [loading, setLoading] = useState(true); useEffect(() => { const timer = setTimeout(() => { setLoading(true); fetch(`/api/admin/students?search=${encodeURIComponent(search)}`).then((r) => r.json()).then((b) => { setStudents(b.data || []); setLoading(false); }); }, 250); return () => clearTimeout(timer); }, [search]); return <AdminShell><header className="page-title"><p className="eyebrow">Student management</p><h1>Students</h1><input className="search-input" placeholder="Search by name or Student ID…" value={search} onChange={(e) => setSearch(e.target.value)} /></header><section className="panel"><div className="table-wrap"><table><thead><tr><th>Student ID</th><th>Name</th><th>Programme</th><th>Email</th><th>Outstanding</th><th></th></tr></thead><tbody>{loading ? <tr><td colSpan="6">Searching…</td></tr> : students.length ? students.map((s) => <tr key={s.student_id}><td>{s.student_id}</td><td>{s.name}</td><td>{s.programme}</td><td>{s.email}</td><td>{money(s.outstanding_balance)}</td><td><Link href={`/admin/students/${s.student_id}`}>View</Link></td></tr>) : <tr><td colSpan="6" className="empty">No matching students.</td></tr>}</tbody></table></div></section></AdminShell>; }
